@{
    ViewData["Title"] = "Classes";
}

@Html.AntiForgeryToken()

<div class="adm-page">
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="adm-alert adm-alert--success mb-3">
            <i class="bi bi-check-circle-fill me-2"></i> @TempData["SuccessMessage"]
        </div>
    }

    <div class="adm-header">
        <div class="adm-header__left">
            <div class="adm-header__icon-wrap">
                <i class="bi bi-building"></i>
            </div>
            <div>
                <h1 class="adm-header__title">Classes</h1>
                <p class="adm-header__sub">Manage school classes, sections, and student capacity</p>
            </div>
        </div>
        <a href="/SchoolClass/CreateEdit" class="adm-btn adm-btn--primary">
            <i class="bi bi-plus-circle"></i> <span>Add Class</span>
        </a>
    </div>

    <div class="adm-filters mb-3">
        <div class="adm-filters__row">
            <div class="adm-filters__field adm-filters__field--search">
                <label class="adm-label">Search</label>
                <div class="adm-input-wrap">
                    <i class="bi bi-search adm-input-wrap__icon"></i>
                    <input type="text" id="table-search" class="adm-input" placeholder="Search by name or code..." />
                </div>
            </div>
            <div class="adm-filters__field">
                <label class="adm-label">Status</label>
                <select id="status-filter" class="adm-select">
                    <option value="">All Statuses</option>
                    <option value="active">Active</option>
                    <option value="inactive">Inactive</option>
                </select>
            </div>
            <div class="adm-filters__field adm-filters__field--actions">
                <label class="adm-label">Action</label>
                <button id="btn-reset" class="adm-btn adm-btn--ghost">Reset Filters</button>
            </div>
        </div>
    </div>

    <div class="adm-table-card">
        <div class="adm-table-card__body">
            <div id="class-table"></div>
        </div>
    </div>
</div>

<div class="adm-modal-backdrop d-none" id="deleteModalBackdrop">
    <div class="adm-modal">
        <div class="adm-modal__header">
            <div class="adm-modal__header-left">
                <div class="adm-modal__header-icon adm-modal__header-icon--danger">
                    <i class="bi bi-trash"></i>
                </div>
                <div>
                    <h5 class="adm-modal__title">Confirm Delete</h5>
                    <p class="adm-modal__subtitle">This action cannot be undone</p>
                </div>
            </div>
            <button type="button" class="adm-modal__close" id="closeDeleteModal">
                <i class="bi bi-x-lg"></i>
            </button>
        </div>
        <div class="adm-modal__body">
            Are you sure you want to delete <strong id="deleteName"></strong>?
        </div>
        <div class="adm-modal__footer">
            <button class="adm-btn adm-btn--ghost" id="cancelDeleteBtn">Cancel</button>
            <button class="adm-btn adm-btn--danger" id="confirmDeleteBtn">
                <i class="bi bi-trash"></i> Delete
            </button>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        var pendingDeleteId = null;
        var deleteModal = document.getElementById('deleteModalBackdrop');

        function openDeleteModal(name, id) {
            pendingDeleteId = id;
            document.getElementById('deleteName').textContent = name;
            deleteModal.classList.remove('d-none');
        }

        function closeDeleteModal() {
            deleteModal.classList.add('d-none');
            pendingDeleteId = null;
        }

        document.getElementById('closeDeleteModal').addEventListener('click', closeDeleteModal);
        document.getElementById('cancelDeleteBtn').addEventListener('click', closeDeleteModal);

        var table = new Tabulator("#class-table", {
            height: false,
            layout: "fitColumns",
            movableColumns: false,
            pagination: true,
            paginationMode: "remote",
            ajaxURL: '@Url.Action("GetList", "SchoolClass")',
            paginationSize: 10,
            paginationSizeSelector: [10,25,50,100],
            paginationDataSent: { "page": "page", "size": "pageSize" },
            ajaxResponse: function(url, params, response) {
                return { data: response.data, last_page: response.last_page };
            },
            columns: [
                { title: "Sort", field: "sortOrder", width: 80, hozAlign: "center", cssClass: "fw-semibold text-muted" },
                {
                    title: "Class Name", field: "name", widthGrow: 2,
                    formatter: function(cell) {
                        return `<div class="entity-cell__name">${cell.getValue()}</div>`;
                    }
                },
                { title: "Code", field: "code", width: 120, hozAlign: "center", cssClass: "adm-cell-mono" },
                {
                    title: "Sections", field: "sectionCount", width: 110, hozAlign: "center",
                    formatter: function(cell) {
                        var id = cell.getRow().getData().id;
                        return `<a href="/Section/Index?classId=${id}" class="adm-badge adm-badge--info text-decoration-none">${cell.getValue()} Sections</a>`;
                    }
                },
                {
                    title: "Students", field: "studentCount", width: 110, hozAlign: "center",
                    formatter: function(cell) {
                        return `<span class="adm-badge adm-badge--success">${cell.getValue()} Students</span>`;
                    }
                },
                {
                    title: "Status", field: "isActive", width: 110, hozAlign: "center",
                    formatter: function(cell) {
                        return cell.getValue()
                            ? '<span class="adm-status adm-status--approved">Active</span>'
                            : '<span class="adm-status adm-status--rejected">Inactive</span>';
                    }
                },
                {
                    title: "Actions", field: "id", hozAlign: "center", headerSort: false, width: 160,
                    formatter: function(cell) {
                        var d = cell.getRow().getData();
                        return buildActionGroup([
                            { url: '/SchoolClass/Details/' + d.id, className: 'action-btn--view', icon: 'eye', title: 'View' },
                            { url: '/SchoolClass/CreateEdit/' + d.id, className: 'action-btn--edit', icon: 'pencil', title: 'Edit' },
                            { className: 'action-btn--delete', icon: 'trash', title: 'Delete', onClick: 'openDeleteModal("' + d.name.replace(/"/g, '&quot;') + '", ' + d.id + ')' }
                        ]);
                    }
                }
            ],
            placeholder: "<div class='dash-empty'><i class='bi bi-building fs-1'></i><div class='dash-empty__title'>No Classes Found</div><div class='dash-empty__sub'>Try adjusting your filters.</div></div>"
        });

        function reloadTable() { table.setData(); }

        document.getElementById('btn-reset').addEventListener('click', function() {
            document.getElementById('table-search').value = '';
            document.getElementById('status-filter').value = '';
            reloadTable();
        });

        document.getElementById('table-search').addEventListener('keyup', function(e) {
            if (e.key === "Enter") reloadTable();
        });

        document.getElementById('status-filter').addEventListener('change', reloadTable);

        document.getElementById('confirmDeleteBtn').addEventListener('click', function() {
            if (!pendingDeleteId) return;
            fetch(`/SchoolClass/DeleteConfirmed/${pendingDeleteId}`, {
                method: 'POST',
                headers: {
                    'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]')?.value
                }
            }).then(function(res) {
                closeDeleteModal();
                reloadTable();
            });
        });
    </script>
}
