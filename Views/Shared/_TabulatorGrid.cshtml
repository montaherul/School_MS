@* 
    _TabulatorGrid.cshtml — Universal Tabulator Grid Wrapper
    Provides the standard grid container markup.
    The grid is initialized via SchoolMS.Grid.create() in the view's Scripts section.
    Usage:
        <div class="adm-table-card adm-grid">
            <div class="adm-table-card__body">
                <div id="data-table"></div>
            </div>
        </div>
*@
