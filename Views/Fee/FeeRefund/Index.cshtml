@{
    ViewData["Title"] = "Fee Refund";
}

<div class="adm-page">
    <div class="adm-header">
        <div class="adm-header__left">
            <div class="adm-header__icon-wrap">
                <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                    <path d="M21 15a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V9a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
                    <path d="M17 19v-6M17 11V5"></path>
                </svg>
            </div>
            <div>
                <h1 class="adm-header__title">Fee Refund</h1>
                <p class="adm-header__sub">Manage fee refunds and reversals</p>
            </div>
        </div>
        <div class="adm-header__right">
            <a href="/FeeRefund/CreateEdit" class="adm-btn adm-btn--primary">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                    <line x1="12" y1="5" x2="12" y2="19"></line>
                    <line x1="5" y1="12" x2="19" y2="12"></line>
                </svg>
                Record Refund
            </a>
        </div>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="adm-alert adm-alert--success">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
                <polyline points="22,4 12,14 8,10"></polyline>
            </svg>
            @TempData["SuccessMessage"]
        </div>
    }

    <div class="adm-filters">
        <div class="adm-filters__row">
            <div class="adm-filters__field adm-filters__field--search">
                <label class="adm-label">Search Refunds</label>
                <div class="adm-input-wrap">
                    <svg class="adm-input-wrap__icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                        <circle cx="11" cy="11" r="8"></circle>
                        <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
                    </svg>
                    <input type="text" id="refundSearch" class="adm-input" placeholder="Search by invoice or student...">
                    <button type="button" class="adm-input-wrap__btn" id="refundSearchBtn">
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                            <circle cx="11" cy="11" r="8"></circle>
                            <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
                        </svg>
                    </button>
                </div>
            </div>
            <div class="adm-filters__field adm-filters__field--actions">
                <label class="adm-label">Actions</label>
                <button type="button" class="adm-btn adm-btn--ghost" id="refreshRefundBtn">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                        <polyline points="23,4 23,10 17,10"></polyline>
                        <path d="M20.49,15a9,9,0,1-2.12,6.32A9,9,0,0,1,4.64,6.32"></path>
                    </svg>
                    Refresh
                </button>
            </div>
        </div>
    </div>

    <div class="adm-table-card">
        <div class="adm-table-card__body">
            <div id="refund-table"></div>
        </div>
    </div>

    <div class="adm-modal-backdrop" id="deleteModal" style="display: none;">
        <div class="adm-modal">
            <div class="adm-modal__header">
                <div class="adm-modal__header-left">
                    <div class="adm-modal__header-icon adm-modal__header-icon--danger">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                            <path d="M10.29,13.29,16.59,7"></path>
                            <circle cx="12" cy="12" r="10"></circle>
                            <line x1="12" y1="2" x2="12" y2="4"></line>
                            <line x1="20" y1="12" x2="18" y2="12"></line>
                            <line x1="12" y1="20" x2="12" y2="22"></line>
                            <line x1="4" y1="12" x2="2" y2="12"></line>
                            <line x1="17" y1="17" x2="7" y2="7"></line>
                        </svg>
                    </div>
                    <div>
                        <h6 class="adm-modal__title">Confirm Delete</h6>
                        <p class="adm-modal__subtitle">This action cannot be undone</p>
                    </div>
                </div>
                <button type="button" class="adm-modal__close" onclick="deleteModal.hide()">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                        <line x1="18" y1="6" x2="6" y2="18"></line>
                        <line x1="6" y1="6" x2="18" y2="18"></line>
                    </svg>
                </button>
            </div>
            <div class="adm-modal__body">
                <p>Are you sure you want to delete refund for invoice <strong id="deleteName"></strong>? This will permanently remove the refund record.</p>
            </div>
            <div class="adm-modal__footer">
                <button type="button" class="adm-btn adm-btn--ghost" onclick="deleteModal.hide()">Cancel</button>
                <button type="button" class="adm-btn adm-btn--danger" id="confirmDeleteBtn">Delete Refund</button>
            </div>
        </div>
    </div>

    @section Scripts {
        <script>
            var pendingDeleteId = null;
            var deleteModal = {
                show: function() {
                    document.getElementById('deleteModal').style.display = 'flex';
                    document.getElementById('deleteModal').classList.add('adm-loading--visible');
                },
                hide: function() {
                    document.getElementById('deleteModal').style.display = 'none';
                    document.getElementById('deleteModal').classList.remove('adm-loading--visible');
                }
            };

            var table = new Tabulator("#refund-table", {
                height: "calc(100vh - 300px)",
                layout: "fitColumns",
                pagination: true,
                paginationMode: "remote",
                ajaxURL: '@Url.Action("GetList", "FeeRefund")',
                paginationSize: 10,
                ajaxResponse: function(url, params, response) {
                    console.log("FeeRefund API Payload:", response);
                    const rows = Array.isArray(response.data) ? response.data : [];
                    return {
                        last_page: response.last_page || 1,
                        total_records: response.total_records || rows.length,
                        data: rows
                    };
                },
                columns: [
                    { title: "Invoice No", field: "invoiceNo", widthGrow: 1.5, formatter: function(cell) {
                        return `<div class="adm-cell-name">${cell.getValue()}</div>`;
                    }},
                    { title: "Student", field: "studentName", widthGrow: 2, formatter: function(cell) {
                        return cell.getValue();
                    }},
                    { title: "Refund Amount", field: "refundAmount", width: 150, hozAlign: "right", formatter: function(cell) {
                        return `<div class="fw-bold" style="color: var(--adm-danger);">${cell.getValue()}</div>`;
                    }},
                    { title: "Method", field: "refundMethod", width: 130, hozAlign: "center", formatter: function(cell) {
                        var methods = { 1: 'Cash', 2: 'Bank', 3: 'Card', 4: 'Mobile Banking', 5: 'Online' };
                        var val = cell.getValue();
                        return `<span class="adm-badge adm-badge--neutral px-3 rounded-pill">${methods[val] || 'Unknown'}</span>`;
                    }},
                    { title: "Reference", field: "referenceNo", widthGrow: 1, formatter: function(cell) {
                        return cell.getValue() || `<span class="text-muted">—</span>`;
                    }},
                    { title: "Approved", field: "isApproved", width: 100, hozAlign: "center", formatter: function(cell) {
                        return cell.getValue()
                            ? `<span class="text-success"><i class="bi bi-check-circle-fill"></i></span>`
                            : `<span class="text-warning"><i class="bi bi-clock-fill"></i></span>`;
                    }},
                    { title: "Refund Date", field: "refundDate", width: 140, hozAlign: "center", formatter: function(cell) {
                        return cell.getValue() ? new Date(cell.getValue()).toLocaleDateString('en-IN') : '';
                    }},
                    {
                        title: "Actions",
                        field: "id",
                        hozAlign: "right",
                        headerSort: false,
                        width: 140,
                        formatter: function(cell) {
                            var id = cell.getValue();
                            return `
                                <div class="adm-actions">
                                    <a href="/FeeRefund/Edit/${id}" class="adm-action adm-action--edit" title="Edit">
                                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                                            <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path>
                                            <path d="M18.5,2.5a2.121,2.121,0,0,1,3,3L12,15l-4,1,1-4Z"></path>
                                        </svg>
                                    </a>
                                    <button class="adm-action adm-action--delete delete-btn" title="Delete" data-id="${id}">
                                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                                            <polyline points="3,6 5,6 21,6"></polyline>
                                            <path d="M19,6v14a2,2,0,0,1-2,2H7a2,2,0,0,1-2-2V6m3,0V4a2,2,0,0,1,2-2h4a2,2,0,0,1,2,2V6"></path>
                                        </svg>
                                    </button>
                                </div>
                            `;
                        },
                        cellClick: function(e, cell) {
                            if (e.target.closest('.delete-btn')) {
                                pendingDeleteId = cell.getValue();
                                document.getElementById('deleteName').textContent = cell.getRow().getData().invoiceNo;
                                deleteModal.show();
                            }
                        }
                    }
                ],
                placeholder: "<div class='adm-loading adm-loading--visible'><div class='adm-loading__spinner'></div><div class='adm-loading__text'>Loading refunds...</div></div>"
            });

            document.getElementById('refundSearchBtn').addEventListener('click', function() {
                var search = document.getElementById('refundSearch').value;
                table.setData('@Url.Action("GetList", "FeeRefund")', { search: search });
            });

            document.getElementById('refundSearch').addEventListener('keypress', function(e) {
                if (e.key === 'Enter') document.getElementById('refundSearchBtn').click();
            });

            document.getElementById('refreshRefundBtn').addEventListener('click', function() {
                table.setData();
            });

            document.getElementById('confirmDeleteBtn').addEventListener('click', function() {
                if (!pendingDeleteId) return;
                fetch(`/FeeRefund/DeleteConfirmed/${pendingDeleteId}`, {
                    method: 'POST',
                    headers: { 'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]')?.value }
                }).then(res => { deleteModal.hide(); table.setData(); });
            });
        </script>
    }
</div>
