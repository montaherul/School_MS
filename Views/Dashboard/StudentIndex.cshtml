@model SchoolManagementSystem.Models.ViewModels.Dashboard.StudentDashboardViewModel
@using System.Text.Json
@{
    ViewData["Title"] = "Student Portal";
}

<div class="row g-4 mb-4">
    <div class="col-12">
        <div class="card border-0 bg-primary text-white p-4 rounded-4 shadow-sm">
            <div class="d-flex align-items-center">
                <div class="me-4">
                    <i class="bi bi-person-badge" style="font-size: 3rem;"></i>
                </div>
                <div>
                    <h2 class="mb-0">Welcome, @Model.FullName</h2>
                    <p class="mb-0 opacity-75">ID: @Model.StudentNo | @Model.ClassName - @Model.SectionName (Roll: @Model.RollNumber)</p>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row g-4">
    <!-- Attendance Card -->
    <div class="col-md-4">
        <div class="card border-0 shadow-sm rounded-4 h-100">
            <div class="card-body text-center p-4">
                <div class="rounded-circle bg-success bg-opacity-10 text-success d-inline-flex align-items-center justify-content-center mb-3" style="width:60px;height:60px;">
                    <i class="bi bi-calendar-check" style="font-size: 1.5rem;"></i>
                </div>
                <h5 class="text-muted small text-uppercase fw-bold">Attendance</h5>
                <h2 class="display-6 fw-bold mb-0">@Model.AttendancePercentage%</h2>
                <p class="text-muted small">Total Attendance</p>
                <div class="progress mt-3" style="height: 8px;">
                    <div class="progress-bar bg-success" role="progressbar" style="width: @Model.AttendancePercentage%"></div>
                </div>
            </div>
        </div>
    </div>

    <!-- Fees Card -->
    <div class="col-md-4">
        <div class="card border-0 shadow-sm rounded-4 h-100">
            <div class="card-body text-center p-4">
                <div class="rounded-circle bg-danger bg-opacity-10 text-danger d-inline-flex align-items-center justify-content-center mb-3" style="width:60px;height:60px;">
                    <i class="bi bi-currency-dollar" style="font-size: 1.5rem;"></i>
                </div>
                <h5 class="text-muted small text-uppercase fw-bold">Outstanding Fees</h5>
                <h2 class="display-6 fw-bold mb-0">৳@Model.TotalDue</h2>
                <p class="text-muted small">Total Due Amount</p>
                <a href="/Payment" class="btn btn-outline-danger btn-sm mt-2 rounded-pill">Pay Now</a>
            </div>
        </div>
    </div>

    <!-- Status Card -->
    <div class="col-md-4">
        <div class="card border-0 shadow-sm rounded-4 h-100">
            <div class="card-body text-center p-4">
                <div class="rounded-circle bg-info bg-opacity-10 text-info d-inline-flex align-items-center justify-content-center mb-3" style="width:60px;height:60px;">
                    <i class="bi bi-check-circle" style="font-size: 1.5rem;"></i>
                </div>
                <h5 class="text-muted small text-uppercase fw-bold">Academic Status</h5>
                <h2 class="display-6 fw-bold mb-0">@Model.StudentStatus</h2>
                <p class="text-muted small">Current Enrollment Status</p>
                <span class="badge bg-info text-dark rounded-pill">Session 2026</span>
            </div>
        </div>
    </div>
</div>

<div class="row g-4 mt-1">
    <!-- Upcoming Assignments -->
    <div class="col-md-6">
        <div class="card border-0 shadow-sm rounded-4 h-100">
            <div class="card-header bg-white border-0 p-4 pb-0 d-flex justify-content-between align-items-center">
                <h5 class="mb-0 fw-bold"><i class="bi bi-journal-check me-2 text-primary"></i>Assignments</h5>
                <a href="/Assignment" class="btn btn-link btn-sm text-decoration-none">View All</a>
            </div>
            <div class="card-body p-4">
                <div id="assignment-grid"></div>
            </div>
        </div>
    </div>

    <!-- Notice Board -->
    <div class="col-md-6">
        <div class="card border-0 shadow-sm rounded-4 h-100">
            <div class="card-header bg-white border-0 p-4 pb-0 d-flex justify-content-between align-items-center">
                <h5 class="mb-0 fw-bold"><i class="bi bi-megaphone me-2 text-warning"></i>Recent Notices</h5>
                <a href="/Communication" class="btn btn-link btn-sm text-decoration-none">View All</a>
            </div>
            <div class="card-body p-4">
                <div id="notice-grid"></div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        const assignments = @Html.Raw(JsonSerializer.Serialize(Model.UpcomingAssignments));
        const notices = @Html.Raw(JsonSerializer.Serialize(Model.RecentNotices));

        new Tabulator("#assignment-grid", {
            data: assignments,
            layout: "fitColumns",
            height: "280px",
            columns: [
                { title: "Subject", field: "Subject", width: 100, formatter: function(cell) {
                    return `<span class="badge bg-primary-soft text-primary tiny fw-bold">${cell.getValue()}</span>`;
                }},
                { title: "Task", field: "Title", widthGrow: 2, formatter: function(cell) {
                    var deadline = new Date(cell.getData().Deadline);
                    var isUrgent = (deadline - new Date()) / (1000 * 60 * 60 * 24) < 2;
                    var urgentHtml = isUrgent ? `<span class="text-danger ms-1"><i class="bi bi-exclamation-circle-fill"></i></span>` : "";
                    return `<div class="fw-bold small">${cell.getValue()}${urgentHtml}</div><div class="text-muted tiny">Due: ${deadline.toLocaleDateString()}</div>`;
                }}
            ],
            placeholder: "No pending assignments"
        });

        new Tabulator("#notice-grid", {
            data: notices,
            layout: "fitColumns",
            height: "280px",
            columns: [
                { title: "Title", field: "Title", widthGrow: 2, formatter: function(cell) {
                    var date = new Date(cell.getData().At);
                    return `<div class="fw-bold small">${cell.getValue()}</div><div class="text-muted tiny">${date.toLocaleDateString()} • ${cell.getData().Summary || ""}</div>`;
                }}
            ],
            placeholder: "No recent notices"
        });
    </script>
    <style>
        .bg-primary-soft { background-color: rgba(26, 86, 219, 0.1); }
        .tabulator { border: none !important; font-size: 13px; }
        .tabulator .tabulator-header { display: none; }
        .tabulator .tabulator-row { border-bottom: 1px solid #f1f5f9; background: transparent !important; }
        .tabulator .tabulator-row .tabulator-cell { padding: 12px 4px; }
        .tiny { font-size: 11px; }
    </style>
}

<div class="row g-4 mt-1">
    <!-- Quick Links -->
    <div class="col-12">
        <div class="card border-0 shadow-sm rounded-4 bg-light">
            <div class="card-body p-4">
                <h5 class="fw-bold mb-4 text-center">Quick Portal Access</h5>
                <div class="row g-3">
                    <div class="col-md-3">
                        <a href="/Details" class="btn btn-white w-100 text-start shadow-sm rounded-3 p-3">
                            <i class="bi bi-person-circle me-2 text-primary"></i> My Profile
                        </a>
                    </div>
                    <div class="col-md-3">
                        <a href="/Result" class="btn btn-white w-100 text-start shadow-sm rounded-3 p-3">
                            <i class="bi bi-journal-text me-2 text-success"></i> My Results
                        </a>
                    </div>
                    <div class="col-md-3">
                        <a href="/AttendanceRecord" class="btn btn-white w-100 text-start shadow-sm rounded-3 p-3">
                            <i class="bi bi-calendar3 me-2 text-warning"></i> My Attendance
                        </a>
                    </div>
                    <div class="col-md-3">
                        <a href="/FeeStructure" class="btn btn-white w-100 text-start shadow-sm rounded-3 p-3">
                            <i class="bi bi-wallet2 me-2 text-info"></i> Fee Structure
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .tiny { font-size: 0.75rem; }
    .btn-white {
        background: #fff;
        border: 1px solid transparent;
        transition: all 0.2s;
    }
    .btn-white:hover {
        border-color: var(--bs-primary);
        transform: translateY(-2px);
    }
</style>
