@model RoutineTeacherViewModel
@{
    ViewData["Title"] = "My Timetable";
}

<div class="adm-page">
    <div class="adm-header">
        <div class="adm-header__left">
            <div class="adm-header__icon-wrap">
                <i class="bi bi-calendar-week"></i>
            </div>
            <div>
                <h1 class="adm-header__title">My Timetable</h1>
                <p class="adm-header__sub">@Model.TeacherName</p>
            </div>
        </div>
    </div>

    <div class="ws-row ws-g-4 ws-mb-4">
        <div class="ws-col-md-3">
            <div class="dash-stat-card">
                <div class="dash-stat-card__icon dash-stat-card__icon--primary">
                    <i class="bi bi-book"></i>
                </div>
                <div class="dash-stat-card__body">
                    <div class="dash-stat-card__value">@Model.TotalSubjects</div>
                    <div class="dash-stat-card__label">Subjects</div>
                </div>
            </div>
        </div>
        <div class="ws-col-md-3">
            <div class="dash-stat-card">
                <div class="dash-stat-card__icon dash-stat-card__icon--success">
                    <i class="bi bi-clock"></i>
                </div>
                <div class="dash-stat-card__body">
                    <div class="dash-stat-card__value">@Model.TotalPeriodsPerWeek</div>
                    <div class="dash-stat-card__label">Periods/Week</div>
                </div>
            </div>
        </div>
        <div class="ws-col-md-3">
            <div class="dash-stat-card">
                <div class="dash-stat-card__icon dash-stat-card__icon--warning">
                    <i class="bi bi-building"></i>
                </div>
                <div class="dash-stat-card__body">
                    <div class="dash-stat-card__value">@Model.TotalClasses</div>
                    <div class="dash-stat-card__label">Classes</div>
                </div>
            </div>
        </div>
        <div class="ws-col-md-3">
            <div class="dash-stat-card">
                <div class="dash-stat-card__icon dash-stat-card__icon--info">
                    <i class="bi bi-calendar-check"></i>
                </div>
                <div class="dash-stat-card__body">
                    <div class="dash-stat-card__value">@Model.TotalWorkingDays</div>
                    <div class="dash-stat-card__label">Working Days</div>
                </div>
            </div>
        </div>
    </div>

    <div class="adm-card ws-mb-4">
        <div class="adm-card__header">
            <h3>Weekly Timetable</h3>
            <div class="ws-flex ws-gap-2">
                <button type="button" class="adm-btn adm-btn--ghost adm-btn--sm" onclick="window.print()">
                    <i class="bi bi-printer"></i> Print
                </button>
            </div>
        </div>
        <div class="adm-card__body">
            <div id="teacher-routine-grid"></div>
        </div>
    </div>

    @if (Model.TodayClasses != null && Model.TodayClasses.Count > 0)
    {
        <div class="adm-card">
            <div class="adm-card__header">
                <h3>Today's Classes</h3>
            </div>
            <div class="adm-card__body">
                <div class="adm-table-wrapper">
                    <table class="adm-table">
                        <thead class="ws-bg-surface">
                            <tr>
                                <th class="ws-fs-13 ws-py-3">Period</th>
                                <th class="ws-fs-13 ws-py-3">Class</th>
                                <th class="ws-fs-13 ws-py-3">Section</th>
                                <th class="ws-fs-13 ws-py-3">Subject</th>
                                <th class="ws-fs-13 ws-py-3">Room</th>
                                <th class="ws-fs-13 ws-py-3">Time</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var cls in Model.TodayClasses)
                            {
                                <tr>
                                    <td class="ws-fw-600">@cls.PeriodName</td>
                                    <td>@cls.ClassName</td>
                                    <td>@cls.SectionName</td>
                                    <td>@cls.SubjectName</td>
                                    <td>@cls.RoomNo</td>
                                    <td class="adm-cell-mono">@cls.StartTime - @cls.EndTime</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }
</div>

@section Scripts {
    <script>
        var columns = [
            { title: "Period", field: "periodName", width: 100, cssClass: "fw-semibold" },
            { title: "Saturday", field: "sat", widthGrow: 1, hozAlign: "center",
                formatter: function(cell) { return cell.getValue() || '\u2014'; }
            },
            { title: "Sunday", field: "sun", widthGrow: 1, hozAlign: "center",
                formatter: function(cell) { return cell.getValue() || '\u2014'; }
            },
            { title: "Monday", field: "mon", widthGrow: 1, hozAlign: "center",
                formatter: function(cell) { return cell.getValue() || '\u2014'; }
            },
            { title: "Tuesday", field: "tue", widthGrow: 1, hozAlign: "center",
                formatter: function(cell) { return cell.getValue() || '\u2014'; }
            },
            { title: "Wednesday", field: "wed", widthGrow: 1, hozAlign: "center",
                formatter: function(cell) { return cell.getValue() || '\u2014'; }
            },
            { title: "Thursday", field: "thu", widthGrow: 1, hozAlign: "center",
                formatter: function(cell) { return cell.getValue() || '\u2014'; }
            },
            { title: "Friday", field: "fri", widthGrow: 1, hozAlign: "center",
                formatter: function(cell) { return cell.getValue() || '\u2014'; }
            }
        ];

        var table = new Tabulator("#teacher-routine-grid", {
            height: false,
            layout: "fitColumns",
            movableColumns: false,
            data: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.WeeklyGrid)),
            columns: columns,
            placeholder: "<div class='dash-empty'><i class='bi bi-calendar-week ws-fs-48'></i><div class='dash-empty__title'>No Timetable Found</div><div class='dash-empty__sub'>Your timetable has not been published yet.</div></div>"
        });
    </script>
}
