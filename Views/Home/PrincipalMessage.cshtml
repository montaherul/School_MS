@model SchoolManagementSystem.Models.Entities.Website.SchoolSetting
@{
    Layout = "~/Views/Shared/_PublicLayout.cshtml";
    ViewData["Title"] = "Principal's Message";
}

<div class="ws-page-hero">
    <div class="ws-container">
        <h1>Principal's Address</h1>
        <p>Words of inspiration and welcome from our honorable head teacher</p>
    </div>
</div>

<section class="ws-section ws-section--alt">
    <div class="ws-container">
        <div class="ws-row ws-g-6">
            <div class="ws-col-12 ws-col-lg-4 ws-text-center">
                <div class="ws-card ws-p-6 ws-bg-secondary-bg">
                    @if (!string.IsNullOrEmpty(Model.PrincipalImagePath))
                    {
                        <img src="@Model.PrincipalImagePath" class="ws-w-full ws-object-cover ws-mb-4 ws-rounded-lg ws-shadow ws-h-360" alt="Principal" />
                    }
                    else
                    {
                        <div class="ws-flex-center ws-w-full ws-mb-4 ws-text-white ws-h-280 ws-rounded-lg ws-fs-64 ws-bg-tertiary-bg ws-text-muted">
                            <i class="bi bi-person-fill"></i>
                        </div>
                    }
                    <h4 class="ws-fw-700 ws-mb-1">@Model.PrincipalName</h4>
                    <p class="ws-text-accent ws-fw-600 ws-mb-0">Principal & CEO</p>
                    <p class="ws-text-muted ws-fs-14">@Model.SchoolName</p>
                </div>
            </div>

            <div class="ws-col-12 ws-col-lg-8">
                <span class="ws-section__label">From the Principal's Desk</span>
                <h2 class="ws-fs-42 ws-fw-800 ws-mb-4">Message from the Head</h2>
                <div class="ws-accent-divider"></div>

                <div class="ws-text-light ws-fs-16 ws-lh-lg">
                    @if (!string.IsNullOrEmpty(Model.PrincipalMessage))
                    {
                        @Html.Raw(Model.PrincipalMessage.Replace("\r\n", "<br/>").Replace("\n", "<br/>"))
                    }
                    else
                    {
                        <p>Welcome to the web portal of our school. Education is not just about scoring grades; it is a life-long journey of character formulation, value inclusion, and preparation for global challenges. At our school, we offer a rich mix of scientific inquiry, language skills, and moral principles.</p>
                        <p>We encourage every student to think outside the box, participate in sports, and engage in social community works. Together with our highly qualified teachers and support staff, we are dedicated to securing a bright future for every child.</p>
                    }
                </div>

                <div class="ws-mt-8 ws-pt-6 ws-border-top">
                    <h5 class="ws-fw-700 ws-mb-2">Sincerely,</h5>
                    <div class="ws-my-4">
                        <span class="ws-fs-24 ws-fst-italic ws-text-secondary" style="font-family:'Courier New',monospace;">@Model.PrincipalName</span>
                    </div>
                    <h6 class="ws-fw-700 ws-text-muted ws-mb-0">Principal</h6>
                    <span class="ws-fs-14 ws-text-muted">@Model.SchoolName</span>
                </div>
            </div>
        </div>
    </div>
</section>
