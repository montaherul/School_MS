@model SchoolManagementSystem.Models.ViewModels.Exam.ExamDetailsViewModel
@{
    ViewData["Title"] = "Delete Exam";
    var exam = Model.Exam;
}

<div class="adm-page er-page er-page--slide">
    <div class="er-page-header">
        <div class="er-page-header__left">
            <div class="er-page-header__icon">
                <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
                    <polyline points="14 2 14 8 20 8" />
                </svg>
            </div>
            <div>
                <h1 class="er-page-header__title">Delete Exam</h1>
                <p class="er-page-header__sub">Are you sure you want to delete this exam?</p>
            </div>
        </div>
    </div>

    <div class="er-card er-card--elevated er-card--danger-border">
        <div class="er-card__body">
            <div class="er-alert er-alert--danger mb-4">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
                <div>
                    <strong class="er-text-md">This action cannot be undone.</strong>
                    <p class="er-text-sm mt-1">All associated data including schedules and results will be permanently removed.</p>
                </div>
            </div>

            <table class="er-table">
                <tbody>
                    <tr>
                        <th class="er-cell-label">Exam Name</th>
                        <td>@exam.Name</td>
                    </tr>
                    <tr>
                        <th class="er-cell-label">Term</th>
                        <td><span class="er-badge er-badge--neutral">@exam.Term</span></td>
                    </tr>
                    <tr>
                        <th class="er-cell-label">Academic Year</th>
                        <td>@exam.AcademicYearName</td>
                    </tr>
                    <tr>
                        <th class="er-cell-label">Duration</th>
                        <td>@exam.StartsOn.ToString("d") – @exam.EndsOn.ToString("d")</td>
                    </tr>
                    <tr>
                        <th class="er-cell-label">Subjects</th>
                        <td>@exam.SubjectCount subject(s)</td>
                    </tr>
                    <tr>
                        <th class="er-cell-label">Student Results</th>
                        <td>@exam.StudentResultCount result(s)</td>
                    </tr>
                </tbody>
            </table>

            <form method="post" action="@Url.Action("DeleteConfirmed", "Exam", new { id = exam.Id })" class="mt-4 d-flex gap-3">
                @Html.AntiForgeryToken()
                <button type="submit" class="er-btn er-btn--danger">
                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><polyline points="3 6 5 6 21 6" /><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2" /></svg>
                    Delete Permanently
                </button>
                <a href="@Url.Action("Index", "Exam")" class="er-btn er-btn--secondary">Cancel</a>
            </form>
        </div>
    </div>
</div>